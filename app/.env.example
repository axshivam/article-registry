# Solana network: devnet | testnet | mainnet-beta
VITE_NETWORK=devnet

# Optional: custom RPC URL (overrides VITE_NETWORK)
# Use http://127.0.0.1:8899 for a local validator
# VITE_RPC_URL=https://api.devnet.solana.com
